Notes:
- Record metrics every time step


TODO:
- Change return of simulating to include agent states - specifically list of per-agent "histories", where a history is a list of that agent's state. so itd be like [ground_truth_list, agent1_list, agent2_list, etc] where each item is a list of that agents perceived matrices over time
- Add dropdown menu to visualization to display agent ground truths
- make visualization window size dynamic (so 100x100 fits in screen)
- Fire matrix with amount of fire points and spread probability
